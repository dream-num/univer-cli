# Domain Docs

本仓库使用 single-context domain glossary。

探索或修改代码前读取根目录 `CONTEXT.md`，并在输出和文档中使用其中定义的 vocabulary。

`CONTEXT.md` 只定义当前 domain term，不保存 implementation detail、decision history、roadmap 或项目日志。
术语发生变化时直接更新 glossary，并同步修正文档中的非 canonical 表达。
