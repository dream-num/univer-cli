{
  "name": "@univer/collab-gateway-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contract for the collab-gateway worktree server: control-plane types, base64url univerfile encoding, the collaboration-client runtimeConfig builder, and the typed WorktreeControlClient. Consumed by the server and the browser client.",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "rm -rf dist *.tsbuildinfo",
    "example:agent-diff": "node examples/agent-diff-context.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json --noEmit --pretty false"
  },
  "dependencies": {
    "@univerjs/core": "1.0.0-insiders.20260831-796c4f4"
  }
}
